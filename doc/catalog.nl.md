delta list:
	b = pairs list
	res = b.first - b.second

math.
	add,sub,mul,div,mod,pow,sqrt,sqr,int,frac,abs,sin,cos,tan,atan

insight.
	history t->number
	scatter (point)
	pie list->enum

gui-input
	rgb
	enum
	range exponential?
	normal
	number
	int
	text

