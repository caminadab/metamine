; html = (uit/tijd)(moment)
succ = xy → xy + 1

app = requestAnimationFrame(ververs)
ververs = dt → setInnerHtml(uit)

el = setInnerHtml("<canvas></canvas")
ctx = getContext(el)

a = #(objs map (obj → obj(ctx))) > -1
canvas = objs → (a ⇒ "<canvas></canvas>")
;ververs = dt → obj
