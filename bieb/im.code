looptijd = nu - starttijd
app = stduit.schrijf(alsTekst(uit))

;-
!) = n → x
	i := 1
	x := 1
	zolang i < n
		x := 
		-;

;-
init:
	a = 1 + 2

itereer:
	b = looptijd + a
	c = als.tekst b
	d = uitvoer(c)
	app = d

exit:
	sys.exit 0
-;
