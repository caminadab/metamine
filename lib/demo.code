;main = (in.vars, start, toets.neer, toets.neer.begin, toets.neer.eind, nu, canvas2d, canvas3d) → (uit, uit.vars)
main = (in.vars, start, nu, in.element) → (uit.vars, uit)

;teken = z → (canvas.clear ∘ z)(canvas.context canvas)
;vierkant = x,y,r → (c → canvas.fillRect(c,x,y,r,r))
