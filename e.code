tag = (a,b) → "<" || a || ">" || b || "</" || a || ">"

head = c → tag("head", c)
html = d → tag("html", d)

pagina1 = html [head "hoi"]
pagina = tag("hoi", "ha")

;uit = sluit(schrijf(stduit, pagina))
uit = pagina1
