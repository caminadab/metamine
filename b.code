uit = "hoi"
;pos = (0..8) × (0..8)
;vlak = p → vierkant(p₀, p₁, 1/8)
;uit = canvas(pos map vlak)

;-
venster = glfwMakeContextCurrent(c.glfwCreateWindow(640, 480, "Venster", 0, 0))

zolang niet glfwWindowShouldClose(venster)
	als ¬ glfwWindowShouldClose dan
		venster = poll(venster swap clear(venster'))
anders
	uit = glfwTerminate
eind
-;
