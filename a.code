standaarduitvoer = kop || data

; kop
;kop = cat [
;	regel  "HTTP/1.0 200 OK"
;	regel  "Host: localhost"
;	regel  "Server: Taal 0.1.0"
;	regel ("Content-Length: " || tekst(#data))
;]
kop = cat [ regel  "HTTP/1.0 200 OK", regel  "Host: localhost", regel  "Server: Taal 0.1.0", regel ("Content-Length: " || tekst(#data)) ]
	
regel = text → text || [13,10] ; regeleinde

; vertaal
code = standaardinvoer
waardeer = ontleed ∘ oplos ∘ (★ "uit") ∘ doe
data = tekst(waardeer(code))
