exitcode = 2 + 3 · a
als ja dan
	a = 8
anders
	a = 1 / 3
stduit = "hoi\n"

; bd := open("dat")
; bd schrijf "hoi"

; bd := open-schrijf "uit"
; dat := lees stdin
; bd := schrijf(bd, dat)



; io sched
; delta
; lifetime
; l(2) = *
; l(bd) = eerste(lees/schrijf(bd)) tot laatste("")
