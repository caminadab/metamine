b := 2
uit = b

;-

a |:= (start, 1)
a |:= (toetsRechts, 2)

als t > 0 dan
	a = 3
anders
	a = 2
eind

a |= (t > 0 ⇒ 3)
a |= (t > 2 ⇒ 2)

a = (t > 0 ⇒ 3) | (t > 2 ⇒ 2)

-;

;-
a = var { (start, 1), (toetsRechts, 2) }
-;


;-
a |:= (start, 0)
a |:= (wanneer(toetsRechts), (toetsRechts ⇒ 2))

als looptijd > 2 dan
	a := 3
eind

a |:= (wanneer(looptijd > 2), 3)


wanneer: T → (moment → T)

-;
