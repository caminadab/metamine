bencB = w → d
w:lijst ⇒  d = "l" || cat(w map bencA) || "e"
w:int   ⇒  d = "i" || tekst w || "e"

; iteratief verdiepen
verdiep = bencA → bencB
benc = (verdiep^1000) (x → [])

in = [1,[2],3,[],[[[]],4]]
uit = benc in
