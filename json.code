waarde = [1,2,[3]]
uit = json waarde

; json encode
json = w → jint | jlijst
jint   = (w:int   ⇒ tekst(w))
jlijst = (w:lijst ⇒ "[" || ((w map json) cat ",") || "]")

json = recursief json-it


; recursief = json-it(zelf,w) → json(w)
