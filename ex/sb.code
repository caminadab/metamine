; hele veld is ~180
l := (45, 0)
r := (135, 0)

speler = pos → boog(pos, 10, τ/2, τ)
net = rechthoek((88,0),(92,20))

als toetsLinks dan r := r' vplus (-1, 0) eind
als toetsRechts dan r := r' vplus (1, 0) eind

als toetsA dan l := l' vplus (-1, 0) eind
als toetsD dan l := l' vplus (1, 0) eind

;x = (Vx,Vy) → Vx
vplus = (vx,vy),(wx,wy) → vx+wx,vy+wy

uit = teken [speler l, speler r, net]
